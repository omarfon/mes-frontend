import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  standalone: true,
  selector: 'app-quality-shell',
  imports: [RouterModule],
  templateUrl: './quality-shell.html',
})
export class QualityShellComponent {}
