import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  standalone: true,
  selector: 'app-traceability-shell',
  imports: [RouterModule],
  templateUrl: './traceability-shell.html',
})
export class TraceabilityShellComponent {}
